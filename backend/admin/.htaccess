# Allow direct access to admin panel files
<IfModule mod_rewrite.c>
    RewriteEngine Off
</IfModule>

# Allow access to all files in admin directory
<FilesMatch "\.(php|html|css|js|png|jpg|jpeg|gif|ico|svg)$">
    Order allow,deny
    Allow from all
</FilesMatch>
